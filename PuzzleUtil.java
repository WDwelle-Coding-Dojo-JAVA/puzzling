import java.util.Random;
public class PuzzleUtil {
    int getTenRolls(int[] roles){
        Random randMachine = new Random();
        int[] arr = new int[10];
        for (int i = 0; i < arr.length; i++) {
            arr[i] = rd.nextInt();
            System.out.println(arr[i]);
            }
    }
}